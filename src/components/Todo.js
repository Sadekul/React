import React from "react";

const Todo = (props) => {
    const {title} = props;
    
      return (
        <div>
          <h2>{title}</h2>
        </div>
      )
    }

    export default Todo;
    